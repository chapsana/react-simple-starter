import React, { Fragment } from 'react';

const Home = () => (
	<Fragment>
		<p className='lead'>
			This is a standard create-react-app with some modifications
		</p>
	</Fragment>
);

export default Home;
